export {
  firebaseConfig,
  firebaseEnv,
  FIREBASE_ENV_KEYS,
  getFirebaseClientConfig,
  isFirebaseConfigured,
} from '@/src/core/config/firebase';

export type { FirebaseClientConfig, FirebaseEnvKey } from '@/src/core/config/firebase';
